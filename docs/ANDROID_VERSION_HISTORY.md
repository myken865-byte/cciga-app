# CCIGA App — Historique des versions Android

_Mandat "Automatisation Google Play" (2026-09-07), §7 — Versioning automatique. Chaque ligne correspond à un AAB/APK réellement produit. Mis à jour à chaque nouveau build, jamais réécrit rétroactivement._

| versionCode | versionName | Commit Git | Date | Environnement | Track Google Play | Résultat |
|---|---|---|---|---|---|---|
| 1 | 1.0 | (non tracé — build initial, avant la mise en place de cet historique) | — | Local / test-builds | Aucun (jamais uploadé) | N/A |
| 2 | 1.0 | 0cdd48c (correctif signature) | 2026-09-08 | CI GitHub Actions (`google-play-internal-test-upload.yml`) | **Internal Test** | ✅ **Premier upload réel réussi** — edit `15161737921180393799`, commité et confirmé live sur le track. SHA-256 `a5bb839f82cab9d05bfa53e963d562304d69f6c8028523a75a5fe21056100059`, 6 172 070 octets. |
| 3 | 1.1 | e9976c7 (freeze version testeur 1.1 — Phases C1-C3 + correctifs NotoSans/DEV-BYPASS) | 2026-09-08 | CI GitHub Actions (`google-play-internal-test-upload.yml`, run [34271644690](https://github.com/myken865-byte/cciga-app/actions/runs/34271644690)) | **Internal Test** | ✅ Upload réussi et confirmé live (mandat "Distribution contrôlée du lien testeur"). SHA-256 `d9cb7a170bae0bae608d9b4544531497828a6e6ee926c4dc8fe7cb82154a00ee`, 6 230 968 octets — identique bit-à-bit au build produit par `android-release-aab.yml` (run 34269931666). **Anomalie découverte après upload** : `capacitor.config.ts` embarquait encore `server.url` = Production (`cciga-app.vercel.app`), restée obsolète depuis un rollback antérieur cette session (jamais rattrapée) — l'app Android est un client léger pur, l'interface réellement servie aux testeurs ne reflétait donc pas les Phases C1-C3 malgré l'upload réussi. Corrigé par versionCode 4 (ligne suivante), voir `docs/ANDROID_RELEASE_CONFIG.md` §9. |
| 4 | 1.1 | e1bea0b (fix: point tester build at DEVTEST — Production is stale) | 2026-09-08 | CI GitHub Actions (`android-release-aab.yml` run [34281519196](https://github.com/myken865-byte/cciga-app/actions/runs/34281519196) puis `google-play-internal-test-upload.yml` run [34281812948](https://github.com/myken865-byte/cciga-app/actions/runs/34281812948)) | **Internal Test** | ✅ Upload réussi, confirmé seule release active sur le track (versionCode 3 entièrement remplacé). SHA-256 `1108d6cdc998767ee09d9f0ab7a86140b3d372cf9d96f3a05b093545fa94513f` — identique entre les deux runs CI (build reproductible). `capacitor.config.ts` pointe **temporairement** vers `https://cciga-app-devtest.vercel.app` (mandat "Option 2 confirmée : DEVTEST/PREPROD") — vérifié embarqué exactement tel quel dans l'AAB (`base/assets/capacitor.config.json`) par le nouveau garde-fou CI, qui compare désormais l'URL réellement embarquée à celle committée au lieu de refuser toute URL devtest par principe. Restauration vers Production documentée dans `docs/ANDROID_RELEASE_CONFIG.md` §9. |

## Règle de progression

- Le prochain AAB **réellement envoyé à Google Play** (n'importe quel track) doit utiliser un versionCode strictement supérieur à **4** (déjà mis à jour dans les deux workflows après cet upload confirmé).
- `versionCode` doit être strictement croissant, sans exception, y compris entre deux tracks différents (Internal Testing puis Closed Testing partagent la même séquence).
- Ne jamais réutiliser un `versionCode` déjà listé ci-dessus, même si l'upload correspondant a échoué côté Google Play — incrémenter et réessayer.

## État de la clé de signature (Play App Signing)

- versionCode 2 a été uploadé avec succès le 2026-09-08 sur le track Internal Test — Play App Signing (déjà actif côté Play Console) a désormais enregistré une empreinte de certificat de clé d'importation à partir de ce premier bundle.
- Le keystore de release (`cciga-release.keystore`) n'est pas conservé en clair dans le dépôt ni sur la machine de développement en dehors d'une fenêtre de build : il est stocké encodé en base64 dans le secret GitHub Actions `ANDROID_RELEASE_KEYSTORE_BASE64` (+ `ANDROID_KEYSTORE_PASSWORD`, `ANDROID_KEY_ALIAS`, `ANDROID_KEY_PASSWORD`), écrit temporairement sur le disque par `.github/workflows/android-release-aab.yml` puis supprimé explicitement après chaque build (« defense in depth »). C'est la source de vérité pour toute signature de release.
